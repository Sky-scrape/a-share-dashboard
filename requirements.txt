# A股看板统一依赖（python -m pip install -r requirements.txt）
requests>=2.28          # 全球总览 / 轮动抓取
numpy>=1.26             # 量化引擎
pandas>=2.2
pyarrow>=15
akshare>=1.14           # 复盘辅助数据源（大盘指数/监管公告/外围）；缺失时相关模块降级报错
py-mini-racer>=0.6      # 全球总览美股热力图：新浪美股页面 JS 解码（fetch_global._us_daily_closes）
pytest>=8               # python -m pytest quant/tests tests -q
#
# 另一主力数据源 hithink-finance 是 npm 全局 CLI（非 pip 包），必须单独安装：
#   npm install -g hithink-finance
# 缺失时复盘/竞价/轮动/全球的抓取都会在运行期失败（start.py 启动预检会提示）。
