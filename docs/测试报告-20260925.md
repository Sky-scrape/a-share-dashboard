# A股看板 · 测试与审查报告（2026-09-25 批次）

> 执行《docs/测试与审查方案-20260925.md》的实测结果。执行时段：2026-09-25 01:10–01:50
> （周五凌晨，非交易时段，全程未触碰在跑链路与 `.status/` 写路径；测试全部离线 mock）。

## 一、执行环境

- OS：Windows 10.0.26100 x64（Git Bash）；Python 3.14.7
- 被审对象：工作区未提交改动（17 modified + 3 新代码文件，阶段总结 20260925 批）
- 基线：审查前 `python -m pytest quant/tests tests -q` = **237 passed**，`tests/smoke.py` ALL PASS

## 二、逐层结果

| 层 | 内容 | 结果 |
| --- | --- | --- |
| L0 | py_compile 全部改动文件 / 新模块导入 / 调试残留扫描 | ✅ 零报错、零残留 |
| L1 | 存量回归 pytest 237 项 + smoke + landing 自测 | ✅ 全绿 |
| L2 | 三套新测试落盘（netguard 18 / auc_archive 13 / parity 9 = **40 项**） | ✅ 全绿，总计 **277 passed** |
| L3 | 守卫副本扫描 / 口径三方一致 / smoke 增补 | ✅ 完成（发现并修复 F8） |
| L4 | 前端视觉矩阵（5 页 × 2 主题 × 4 宽度） | ⏸ 未执行（CSS-only 改动，需人工/截图过矩阵，见方案 §8 检查点 W1–W7） |
| L5 | exe 双形态验收 | ⏸ 未执行（P2；打包后按方案 §9 清单过） |
| L6 | 运维演练 + 生产闭环 | ◐ 静态部分完成；生产核对留自然窗口（见 §六） |

## 三、声明复核表（S1–S6，全部证实）

| # | 声明 | 实测 | 结论 |
| --- | --- | --- | --- |
| S1 | parity n=447 零不一致 | 重算：447 行、双方不可执行/执行完全一致、**数值不一致 0、单边执行 0**；与旧实现分歧 = 间隙段 (−5,−3] 23 笔（报告口径）+ low≤−5 出场价变更 24 笔 = 47 笔，全部源于本次修正 | ✅ |
| S2 | netguard 边界用例全过 | 固化为 18 项 pytest（`tests/test_netguard.py`），版本稳定地址集，3.11/3.14 双兼容 | ✅（且已入仓） |
| S3 | factors 重建至 20260924 | `data/recap/us_market/factors.json`：**221 行，2025-11-03 → 2026-09-24**；9/23 行 us_date=2026-09-23；sectors 12 项非空 | ✅ |
| S4 | 止损修正不影响 OOS 判定 | `spec_validate.py:263-264`：win_rate/avg_close 直接取 `p["close"]`，与止损口径无耦合 | ✅ |
| S5 | 归档抢救 9/24 数据 | `auc-20260924.json.gz` 61,220B；final 79 / series 21 轮 / rounds_meta 21 / benchmark 6 / sector 90 / watchmap 79，全部与报告吻合 | ✅ |
| S6 | C6 美股闸门不触发 | 9/23 factors：标普 −0.72 / 纳指 −1.13，均 > −2.0 → defensive、闸门不触发；603186 验票记录（o+0.4/h+1.45/l−6.82/c−6.32、risk_hit、归因市场系统性风险）逐字段吻合 | ✅ |

## 四、缺陷清单（发现 4，全部修复并有测试锚定）

| # | 缺陷 | 位置 | 修复 | 回归锚 |
| --- | --- | --- | --- | --- |
| **F1** | 终态抓取异常早退**跳过归档**：当日 live 轮次已在 series.json，次日 09:14 覆盖即永久丢失（与归档立项初衷相悖） | `auc_collector._timeline` 异常分支 | 早退前调 `auc_archive.archive(date8)`（缺失产物按缺省跳过，语义自洽） | `test_tc3_timeline_final_failure_still_archives` |
| **F1b** | 同类绕过：`run_once` 的 fetch_benchmark/AUI 无守卫、两处 `fetch_sector` 无守卫——任一异常都绕过末尾归档 | `auc_collector.run_once/_timeline` | 与 `_timeline` 既有口径对齐：包 try/except [warn]，归档行必达 | `test_tc4`（跳过路径不归档语义不变）+ smoke 结构断言更新 |
| **F2** | `save_status` 整写覆盖 vs `_record_status` 合并写不对称：失败日/非交易日短状态抹掉 archive 段，health 归档可见性抖动 | `auc_config.save_status` | 其余键仍整写（陈旧即误导），仅 archive 段读旧保留（调用方显式携带时以其为准） | `test_tc1` + `test_tc2` |
| **F8** | **守卫第三处副本**：`notify._post` 要求解析结果全部 `is_global`，fake-IP 段被判非公网 → 一旦配置推送，三类告警在代理环境**全部静默失效**（本机未配置故为潜伏缺陷；watchdog 探字面量 127.0.0.1 不受影响，已排除） | `backend/notify.py:69` | netguard 新增 `is_proxy_fakeip()` 豁免判定单一来源，notify 复用 | `test_i1`（放行）+ `test_i2`（私网仍拒） |

**附带修正（auc_archive 自身）**：空产物早退路径原本跳过 `_record_status`（与 F1 同类）——改为 `finally` 落状态，所有出口可见（`test_n3`）。

**接受风险（记录不改）**：
- netguard 第 2 层 TOCTOU（check 与 connect 之间 DNS 可再变）：纵深防御第 2 层本非唯一防线，第 1 层白名单是控制面——维持现状，模块头已自述定位；
- `trust_env` 代理依赖：netguard 模块头「由本机回环代理持有并按 hostname 路由」已覆盖意图。

## 五、新增测试资产（237 → 277，+40 项，全部离线）

| 文件 | 项数 | 覆盖 |
| --- | --- | --- |
| `tests/test_netguard.py` | 18 | 豁免精确 /15、真实私网/环回/链路本地（含云元数据）/保留/组播/IPv6 阻断、协议与白名单（含尾点 fail-closed、大写域名）、fake-IP 放行（原始 bug 回归锚）、禁重定向参数固化、diagnose 容错、notify 调用点复用（F8） |
| `tests/test_auc_archive.py` | 13 | 归档组装/部分缺失/空日不写文件/幂等覆盖/失败不抛/状态合并/损坏状态容错/路径与 int 日期/真实数据复制集成（不触碰生产文件）；F1 早退归档 + 跳过路径不归档 + save_status 语义（TC 组） |
| `tests/test_exec_parity.py` | 9 | 黄金样本 603186（−4.38%）、low=−5 恰命中（`<=` 语义）、新旧分歧固化、NLU 限价不成交、**双实现全网格等价扫描**、三方常量同源、S1 全窗 447 复算（skipif 保护，CI 无数据时自动跳） |
| `tests/smoke.py` 增补 | +2 | `/api/auction/status` 归档段合式（未归档日允许缺省）；sector 两处终态挂接断言更新为「try 兜底 + 归档可达」 |

全部用例零真实网络（DNS/HTTP mock），CI（Python 3.11，无本地数据）可跑；唯一数据依赖 S1 有 skipif。

## 六、遗留与建议

1. **生产闭环核对（自然验收窗口，无需人工触发）**：
   - 今晨 04:05 `arecap-usclose-fetch`：日志应零 `198.18` 报错、factors 落 2026-09-25 行；
   - 今晨 09:25 后：`.status/auction.json` 出现 `archive.date=20260925` 段（修复后首个交易日，含 F1 路径保障）；
   - 傍晚 17:05 `arecap-daily-fetch`：失败明细推送路径（若配置 notify）首验 F8 修复。
2. **L4 前端矩阵**：按方案 §8 W1–W7 人工过（重点：1440px 断点顶栏、轮动页吸顶 `--head-h`、量化/复盘通栏、双主题重绘）。
3. **L5 exe 验收**：下次 `打包exe.bat` 后按方案 §9 清单执行，同时关闭「dist 旧守卫」遗留项；F6（`_fallback_stdio` 两分支）在该清单第 3 条一并实测。
4. **CI 观察**：pywebview 进 requirements 后 ubuntu 矩阵的可装性（F7）；若红，把 pywebview 移入可选注释段（README 已声明仅打包需要）。
5. **口径纪律**：`_val_exec_stats` 与 `_exec_pnl_realtime` 仍是同文件内两处止损判据（本次未动，扫描有三方常量同源测试兜底）——下次触碰 spec_validate 时建议抽公共函数。
6. **提交建议**：本批新增改动 = 4 个缺陷修复（F1/F1b/F2/F8 + 归档 finally）+ 3 个测试文件 + smoke 增补，全部有测试锚定、277 passed + SMOKE ALL PASS，可随原批次一并进入提交流程（未执行 git 操作，留用户决定）。
